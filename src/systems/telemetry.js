export function createTelemetry(){return{startedAt:new Date().toISOString(),scenario:1,casts:{lightning:0,terrace:0,hollow:0},combatTerrainEdits:0,outOfCombatTerrainEdits:0,editRepaths:0,deaths:0,restarts:0,scarReturns:0,elevatedTime:0,elapsed:0,events:[]}}export function logEvent(world,type,payload={}){world.telemetry.events.push({tick:world.tick,t:Number(world.time.toFixed(2)),type,...payload});if(world.telemetry.events.length>3000)world.telemetry.events.shift()}export function terrainCastShare(t){const terrain=t.casts.terrace+t.casts.hollow,total=terrain+t.casts.lightning;return total?terrain/total:0}export function downloadTelemetry(world){const data={...world.telemetry,terrainCastShare:terrainCastShare(world.telemetry),lightningShare:1-terrainCastShare(world.telemetry)},blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`divine-stage1-${Date.now()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),500)}
